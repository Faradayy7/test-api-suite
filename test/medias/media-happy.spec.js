const { test, expect } = require("@playwright/test");
const { ApiClient } = require("../../utils/api-client");
const { logger } = require("../../utils/logger");

test.describe("GET /api/media - Happy paths", () => {
  

  
});
